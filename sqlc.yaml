version: "1"
packages:
  - name: "db"
    path: "internal/db"
    schema: "./sql/schema.sql"
    queries: "./sql/queries.sql"
    engine: "postgresql"
    emit_json_tags: true
    emit_prepared_queries: true